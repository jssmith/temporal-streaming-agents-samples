{
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "test:e2e": "npx playwright test",
    "record:demo": "rm -f test-results/recording/sync.json && npx playwright test --config playwright.recording.config.ts && bash scripts/combine-recordings.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
